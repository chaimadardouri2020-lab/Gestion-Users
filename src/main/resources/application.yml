spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gestion_users?createDatabaseIfNotExist=true
    username: root
    password: VOTRE_MOT_DE_PASSE_MYSQL
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8080

jwt:
  secret: VotreSecretJWTTresLongEt64CaracteresMinimumPourHS256Securite
  expiration: 900000       # 15 minutes
  refresh-expiration: 604800000  # 7 jours